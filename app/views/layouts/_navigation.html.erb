<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Zuwarka", root_path, class: "navbar-brand", id: "logo" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to "Articles", articles_path %></li>
        <li><%= link_to "Create Article", new_article_path %></li>
        <li><%= link_to "Users", users_path, class: "navbar-link" %></li>
      </ul>
      <ul class="nav navbar-right navbar-nav">
        <% if logged_in? %>
        <li>
          <%= button_to "Log out", logout_path, method: :delete, class: "btn-xs btn-danger my_nav-button",
          onclick: "return ask(\"Are you sure?\");"%>
        </li>
        <% else %>
        <li>
          <%= button_to "Sign up", signup_path, method: :get, class: "btn-xs btn-warning my-nav-bar" %>
        </li>
          <li>
            <%= button_to "Log in", login_path, method: :get, class: "btn-xs btn-success my-nav-bar" %>
          </li>
        <% end %>
      </ul>

    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>